CREATE TABLE IF NOT EXISTS crimes (
	  type VARCHAR(150)
		year INT
		month INT
		day INT
		hour INT
		minute INT
		hundred_block CHAR(100)
		neighborhood CHAR(100)
		x VARCHAR(100)
		y VARCHAR(100)
); 
